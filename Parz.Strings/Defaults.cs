using System.Collections.Generic;

namespace Parz.Strings
{
    public static class Defaults
    {
        public static readonly IEnumerable<string> QuotationMarks = new[]
        {
            "\"",
            "'",
            "`"
        };
    }
}
