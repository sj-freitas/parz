namespace Parz.Strings
{
    public static class LambdaTokenType
    {
        /// <summary>
        /// A C# lambda expression type string.
        /// </summary>
        public static readonly TokenType Text = nameof(Text);
    }
}
